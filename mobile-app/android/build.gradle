buildscript {
    repositories {
        google() // Dodaj to
        mavenCentral() // Dodaj to
    }
    dependencies {
        classpath 'com.google.gms:google-services:4.3.15' // Aktualizuj wersję na najnowszą
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
